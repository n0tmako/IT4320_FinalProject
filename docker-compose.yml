version: "3.9"
services:
  bus-reservation:
    build: .
    ports:
      - "5001:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=change_me
    depends_on: []
